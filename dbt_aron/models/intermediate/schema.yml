
version: 2

models:

  - name: my_second_dbt_model
    description: "Intermediate model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
